package com.robot.platform.robot.mission.enums;

/** Stable origin labels are part of the request-id idempotency contract. */
public enum MissionSource { ADMIN, APP, VOICE, AGENT, SCHEDULE, INSPECTION, SYSTEM }
