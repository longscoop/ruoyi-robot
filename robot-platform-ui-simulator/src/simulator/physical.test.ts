import{describe,it,expect}from'vitest';import{NavigationSimulator}from'./navigation';import{InspectionSimulator,perception}from'./perception';describe('physical robot simulation',()=>{it('simulates nav failure',()=>{const n=new NavigationSimulator;n.navigate({x:1,y:2,yaw:0});n.fail('BLOCKED');expect(n.state).toBe('BLOCKED')});it('runs inspection points and anomalies',()=>{const i=new InspectionSimulator;i.load(['客厅','餐桌']);expect(i.next()?.name).toBe('客厅');expect(perception('FALL').kind).toBe('FALL')})})