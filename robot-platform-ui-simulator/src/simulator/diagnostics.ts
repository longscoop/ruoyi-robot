import type{TraceStore}from'./core';export class FaultInjector{latencyMs=0;drop=false;duplicate=false;outOfOrder=false;async deliver<T>(value:T,fn:(v:T)=>void){if(this.drop)return;await new Promise(r=>setTimeout(r,this.latencyMs));fn(value);if(this.duplicate)fn(value)}}export function downloadTrace(store:TraceStore){const blob=new Blob([store.export()],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='robot-simulator-trace-'+Date.now()+'.json';a.click();URL.revokeObjectURL(a.href)}