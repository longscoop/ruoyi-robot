import{describe,it,expect}from'vitest';import{IdempotencyGuard,envelope}from'./core';describe('protocol core',()=>{it('uses backend v1 envelope',()=>{const e=envelope('HEARTBEAT',{robotId:1});expect(e.version).toBe(1);expect(e.source).toBe('ROBOT')});it('deduplicates message ids',()=>{const g=new IdempotencyGuard;expect(g.accept('x')).toBe(true);expect(g.accept('x')).toBe(false)})})