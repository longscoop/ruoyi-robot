import{describe,expect,it}from'vitest';function fields(mode:string){return{realtime:mode!=='CASCADE',cascade:mode!=='NATIVE'}}describe('agent route form',()=>{it('maps native/cascade/auto fields',()=>{expect(fields('NATIVE')).toEqual({realtime:true,cascade:false});expect(fields('CASCADE')).toEqual({realtime:false,cascade:true});expect(fields('AUTO')).toEqual({realtime:true,cascade:true})})})